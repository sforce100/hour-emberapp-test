import { showRegion } from '../../../helpers/show-region';
import { module, test } from 'qunit';

module('Unit | Helper | show region');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = showRegion(42);
  assert.ok(result);
});
